export interface Theme {
  colours: {
    primary: string,
    secondary: string,
    background: string,
    text_primary: string
  }
}
export const theme = {
  colours: {
    primary: "#F6BD60",
    secondary: "#5D2A42",
    background: "#0C171C",
    text_primary: "#FFFFFF"
  }
}
import { fromBottom } from './transitions/transitions';

/**
 * Simple implementation
 * add the same custom transition to all transition animation
 */
export const authTransitionConfig = fromBottom;
